import { Heart, ArrowRight } from 'lucide-react';
import { useShoppingPreferences } from '@/context/ShoppingPreferences';
import { useSavedProducts } from '@/lib/useSavedProducts';
import { ProductCard } from '@/components/ProductCard';
export function FavouritesPage(){
  const {favourites}=useShoppingPreferences();const {products,loading,error,retry}=useSavedProducts(favourites);
  return <div className="pt-20 min-h-screen bg-stone-50"><div className="bg-stone-900 py-8"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><h1 className="text-2xl sm:text-3xl font-bold text-white">Të preferuarat</h1><p className="text-sm text-stone-300 mt-2">Orët që të pëlqejnë, të ruajtura në këtë pajisje.</p></div></div><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">{loading?<p role="status" className="text-stone-500">Duke ngarkuar orët…</p>:error?<div role="alert" className="text-center py-12"><p>Nuk mund t’i ngarkojmë orët për momentin.</p><button onClick={retry} className="mt-4 px-5 py-3 bg-amber-700 text-white rounded-xl">Provo përsëri</button></div>:products.length?<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3 sm:gap-6">{products.map(product=><ProductCard product={product} key={product.id}/>)}</div>:<div className="text-center bg-white rounded-2xl border border-stone-100 py-14 px-5"><Heart aria-hidden="true" className="w-10 h-10 text-amber-700 mx-auto mb-4"/><h2 className="text-xl font-bold text-stone-900">Ruaj orën që të pëlqen</h2><p className="text-sm text-stone-500 mt-2 mb-6">Prek zemrën pranë një ore për ta gjetur përsëri këtu.</p><a href="#/shop" className="inline-flex items-center gap-2 px-5 py-3 rounded-xl bg-amber-700 text-white font-semibold">Zbulo koleksionin <ArrowRight size={17}/></a></div>}</div></div>;
}
